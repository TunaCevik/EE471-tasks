FROM python:3.11-slim
WORKDIR /app

# Copy the UI requirements and install them
COPY requirements-ui.txt .
RUN pip install --no-cache-dir -r requirements-ui.txt

COPY . .
EXPOSE 5001
CMD ["python", "app.py"]